/*
#include <iostream>

using namespace std;

int main()
{
	string s;
	string temp;

	cin >> s;
	cin >> temp;

	if (s.find(temp) == -1) {
		cout << -1;
	}
	else if(s.find(temp) != -1){
		cout << s.find(temp);
	}
	return 0;
}
*/